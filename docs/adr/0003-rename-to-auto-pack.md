# Rename Electro to Auto Pack in place

The product and directory rename from Electro (`electro`) to Auto Pack (`auto_pack`) happens in the same repo/path lineage (rename, not a greenfield copy). Console (Electron shell) and existing Fastlane/CLI land in that renamed tree so history and local config stay continuous.
